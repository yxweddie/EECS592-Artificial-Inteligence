## How to run this assignment

1. I was using PyCharm for implementing this assignment, so to run this assignment
you can either import the whole zip (the *.py files and large_maps) into PyChar.
you should be able to run each of python file.

2. to run my assignment in command line, please let the python files and large_maps
in the same directory. And you should go into this directory.
If you want to run A* search, run the following command:
 python A*.py

3. result file contains the result for largemaps.
